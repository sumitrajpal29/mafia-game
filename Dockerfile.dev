# Development Dockerfile with Git
FROM node:18

# Install git and other useful tools
RUN apt-get update && apt-get install -y \
    git \
    vim \
    nano \
    && rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /workspace

# Configure git (you'll need to set your own credentials)
# These are just placeholders - you'll configure them when running the container

# Install global npm packages that might be useful
RUN npm install -g nodemon

# Expose ports for development
EXPOSE 3000 3001

# Keep container running
CMD ["bash"]
